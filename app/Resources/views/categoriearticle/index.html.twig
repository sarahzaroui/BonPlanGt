{% extends "BackBonPlanBackBundle::layout.html.twig" %}
{% block tilte %}
    Administration Bon plan
{% endblock %}
{% block container %}
    <h2>Categoriee article list</h2>
    <div class="col-md-12" style="padding-bottom: 10px;">
        <a href="{{ path('categoriearticle_new') }}"><button class="btn btn-xs btn-success">Create a new catégorie article</button></a>
    </div>
    <table class="table" id="example2">
        <thead>
        <tr>
            <th>Idcatart</th>
            <th>Nom</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for categoriearticle in categoriearticles %}
            <tr>
                <td><a href="{{ path('categoriearticle_show', { 'idcatart': categoriearticle.idcatart }) }}">{{ categoriearticle.idcatart }}</a></td>
                <td>{{ categoriearticle.nom }}</td>
                <td>

                    <a href="{{ path('categoriearticle_show', { 'idcatart': categoriearticle.idcatart }) }}">
                        <button class="btn btn-xs btn-danger"><i class="fa fa-trash"></i></button>
                    </a>

                    <a href="{{ path('categoriearticle_edit', { 'idcatart': categoriearticle.idcatart }) }}">
                        <button class="btn btn-xs btn-primary"><i class="fa fa-edit"></i></button>
                    </a>
                    <a href="{{ path('categoriearticle_show', { 'idcatart': categoriearticle.idcatart }) }}">
                        <button class="btn btn-xs btn-success"><i class="fa fa-eye"></i></button>
                    </a>

                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
